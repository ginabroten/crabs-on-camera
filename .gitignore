.Rproj.user
.Rhistory
.RData
.Ruserdata

# OS
.DS_Store

# Jupyter
.ipynb_checkpoints/

# The small example/test dataset under data/annotations/test_annotations IS
# tracked in git on purpose, so the repo is runnable/reproducible out of the box.
# The full ~8,000 image training set is hosted externally (see docs/technical_doc.Rmd)
# and should NOT be added here — if you add other/larger datasets under data/,
# gitignore them explicitly rather than committing them.

# Script outputs (YOLO exports, trained weights, etc.)
yolo_split/
data/kfold/
runs/
*.pt
