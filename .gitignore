.Rproj.user
.Rhistory
.RData
.Ruserdata

# OS
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Raw image data (large; not tracked in git)
# The small example/test dataset under data/annotations/test_annotations
# is kept for demoing the pipeline, but the actual image files are excluded.
# The full ~8,000 image training set is hosted externally (see docs/technical_doc.Rmd).
data/**/images/
*.jpg
*.jpeg
*.png
*.tif
*.tiff

# Script outputs (YOLO exports, trained weights, etc.)
yolo_split/
runs/
*.pt
